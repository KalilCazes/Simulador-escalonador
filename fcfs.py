#!/usr/bin/env python3.7

import funcoes


def executa(matriz):
    #inicio, fim, prioridade = []
    for processos in range(int(matriz[0])):
        inicio[i] = funcoes.get_inicio(matriz, processos[i])
        fim[i] = funcoes.get_fim(matriz, processos[i])
        prioridade[i] = funcoes.get_prioridade(matriz, processos[i])


        